#include <stdio.h>
/**
 * main - main block
 * Description: prints all single digits of base 10
 * Return: 0
 */
int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
	{
		printf("%d", m);
	}
	putchar('\n')
		return (0)
}
