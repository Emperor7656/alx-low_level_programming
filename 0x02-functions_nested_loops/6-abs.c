#include "main.h"
/**
 * _abs - computes the value of a integer
 *  Return: return 1 if its true
 *  @n: n is an integer
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

