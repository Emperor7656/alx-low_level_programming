#include "main.h"
/**
 * _islower - chescks for lower case character
 * Return: return 1 if lowercase
 * @c: is an ASCII character
 */
int _islower(int c)
{
	if (c = 'a' && c <= 'z')
	{
		return (1);
	}
	{
		else
		return(0);
	}
}
